{
  "name": "docker-tutorial",
  "version": "1.0.0",
  "description": "A boilerplate example to start coding with docker postgres and knex in node environment",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/nodemon server/index.js --ignore node_modules/",
    "knex":"./node_modules/.bin/knex migrate:latest"
  },
  "contributors": [
    "Deepak Sharma"
  ],
  "dependencies": {

    "express": "^4.15.2",
    "knex": "^0.13.0",
    "pg": "^4.4.1"

  }
}
